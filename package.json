{
  "name": "emasgez-export-website",
  "version": "1.0.0",
  "description": "EMASGEZ Elevator — B2B Export Website",
  "scripts": {
    "dev": "npx live-server --port=3000 --open=index.html",
    "start": "npx live-server --port=3000"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}
